# Required: generate with `openssl rand -base64 32`.
MASTER_KEY=

# Optional defaults.
CHECK_INTERVAL_MINUTES=5
REDEEM_LEAD_MINUTES=10

# Upstash Redis and QStash variables are injected by their Vercel integrations.
